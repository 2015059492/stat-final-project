---
title: "Final Project Report"
author: "Thembisile Buthelezi"
format: html
---

# Introduction

This section will introduce the final project and its main purpose.

# Data

This section will describe the data used in the project.

# Methods

This section will explain the statistical methods used in the analysis.

# Results

This section will present the results of the analysis. For now, the code below performs a simple calculation to confirm that Python code runs correctly when the Quarto report is rendered.

```{python}
x = 2 + 2
print("Test calculation:", x)
```
The result should be 4. This confirms that the report can execute Python code successfully.

# Reproducibility

This section records information needed to reproduce the analysis. The code below prints the Python version and operating system used to render the report.

```{python}
import sys
import platform

print("Python version:", sys.version)
print("Operating system:", platform.platform())
```

These details document the computing environment used for the analysis and can help identify differences if the report is run on another machine.
The following check confirms that the main project folders needed for the analysis are present. This helps verify that the repository has the expected structure when it is cloned or moved to another computer.

```{python}
from pathlib import Path

required_folders = ["data", "py", "output"]

for folder in required_folders:
    if Path(folder).exists():
        print(folder, "folder found")
    else:
        print(folder, "folder missing")
```

If all three folders are found, the basic project structure is available for the analysis. If a folder is missing, this check makes the problem visible before later code depends on it.

To test portability, I also rendered this project in a GitHub Codespace, which uses a Linux environment. The first render attempt did not work because Quarto was not installed in the Codespace. I installed Quarto 1.10.18 and the report then rendered successfully. During this test, I also noticed that the empty output folder was not included when the repository was cloned, so I added a .gitkeep file to make sure the folder is preserved. After these changes, the report rendered successfully in the Codespace.